#include <stdio.h>

int main(void)
{
    char a= '?'
    printf("who is the coolest cat in the ENGG1003 course?");
    scanf("%d", a);
    if(a=a) {
        printf(Jason Iredale!!);
    }
    else{
        ("No. It's Jason Iredale!")
    }
    return 0;
}